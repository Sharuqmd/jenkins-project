pipeline {
    agent any
    stages {
        stage('checkout') {
            steps {
                git 'https://github.com/Sharuqmd/jenkins-project.git'
            }
        }
        stage('Build') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}